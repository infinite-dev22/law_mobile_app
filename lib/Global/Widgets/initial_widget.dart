import 'package:flutter/material.dart';

class GlobalInitialWidget extends StatelessWidget {
  const GlobalInitialWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Initial Widget"),
    );
  }
}
