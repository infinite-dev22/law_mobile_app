import 'package:flutter/material.dart';

class NoIssuesWidget extends StatelessWidget {
  const NoIssuesWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("No issues available now"),
    );
  }
}
