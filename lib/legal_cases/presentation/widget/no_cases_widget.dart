import 'package:flutter/material.dart';

class NoCasesWidget extends StatelessWidget {
  const NoCasesWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("No cases available now"),
    );
  }
}
