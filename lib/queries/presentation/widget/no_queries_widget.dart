import 'package:flutter/material.dart';

class NoQueriesWidget extends StatelessWidget {
  const NoQueriesWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("No queries available now"),
    );
  }
}
